# Webpack + Electron + Typescript + React + Mobx

## usage

```sh
npm install
npm run dev
```

## リリース方法

package.json のバージョンを書き換える

`v<バージョン番号>` のタグを作成する。 

```sh
git tag v0.1.1
```

タグを push する

```sh
git push --tags
```
